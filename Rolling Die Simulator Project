# Rolling Die Simulator Project – The dice rolling simulator will imitate the experience of rolling a dice.

#It will generate a random number and the user can play again and again to get a number from the dice until the user decides to quit the program.
print('lets a rolled a dice')
print('enter a number of player')
number_of_player = int(input())
i = 0
h = []

while True:
    if i == number_of_player:
        break
    else:
        print('enter a name of player')
        k = str(input())
        w = h.append(k)
    i = i + 1
print('Players name')
print(h)
import random
print('Rolling Dice')
a = ['1','2','3','4','5','6']
q = -1
while True:
    q = q +1
    print('press 0 for roll a dice or 1 for exit')
    k = int(input())
    if k == 0:
        c = random.choice(a)
    elif k == 1:
        break
    if q == number_of_player:
        q = q - number_of_player
    print(h[q]+ ' your lucky number is : '+ c)
